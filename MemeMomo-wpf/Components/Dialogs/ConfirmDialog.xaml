<dialogs:MemoDialogWindow x:Class="MemeMomo.Components.Dialogs.ConfirmDialog"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:dialogs="clr-namespace:MemeMomo.Components.Dialogs"
                          xmlns:ui="clr-namespace:MemeMomo.UI"
                          Title="确认"
                          Width="300"
                          Height="164"
                          ShellBackground="{DynamicResource SurfacePrimaryBrush}"
                          ShellBorderBrush="{DynamicResource BorderEmphasisBrush}"
                          PreviewKeyDown="OnDialogPreviewKeyDown">
  <Grid Margin="18">
    <Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
    <StackPanel MouseLeftButtonDown="OnTitleBarMouseDown">
      <TextBlock x:Name="TitleText" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
      <TextBlock x:Name="MessageText" Margin="0,8,0,0" FontSize="12.5" LineHeight="18" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" />
    </StackPanel>
    <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
      <Button Height="32" Padding="14,0" Content="取消" ui:CornerRadiusProxy.Value="8" Click="OnCancelClick" />
      <Button x:Name="ConfirmButton" Height="32" Margin="8,0,0,0" Padding="14,0" Foreground="{DynamicResource TextOnAccentBrush}" Content="确定" ui:CornerRadiusProxy.Value="8" Click="OnConfirmClick" />
    </StackPanel>
  </Grid>
</dialogs:MemoDialogWindow>
